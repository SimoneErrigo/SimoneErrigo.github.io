# Simone Errigo — portfolio

Static React portfolio built with Vite and the React Bits Faulty Terminal background.

```bash
npm install
npm run dev
```

Use `npm run build` for the production bundle and `npm run preview` to inspect it locally.

Push to `main` and enable **Settings → Pages → GitHub Actions** to publish it.
